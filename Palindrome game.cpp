#include<iostream>
#include<cstring>
using namespace std;
int palindrome(string ss)
{
	int i;
	for(i=0;i<ss.length();i++)
	{
		if(s[i]!=s[ss.length()-1-i])
		{
			return 0;
		}
		else
		{
			return 1;
		}
	}
}
int main()
{
	string ss,salice,sbob;;
	cin>>ss;
	salice=ss[0];
	for(i=0;i<ss.length();i++)
	{
		
		
	}
		
}
